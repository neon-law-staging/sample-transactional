node_modules/
dist/
coverage/

# Per-machine agent settings. `.agents/launch.json` is committed — it is
# how the dev server is started and belongs to the repository.
.agents/settings.local.json
.agents/worktrees/
*.tsbuildinfo
.DS_Store
.env
.env.*
!.env.example
